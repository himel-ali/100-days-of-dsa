#include <iostream>
#include <vector>
#include <iostream>
#include <algorithm>
#include <climits>
using namespace std;

// Time and Space Complexity
// - Not the actual time taken 
// - but the amount of time taken as function of input size (n)
// Big O Notatiion (uperboud)  O(n)  ---> is for the worst case scenario when the operation is running thorugh all the elements to the end of the data structure

// worst case sceanrio ---> O(big o), upper bound
// avg case scenario --->  Θ(theta)
// best case scenario --->  Ω(omega), lower bound






int main(){


    return 0;
}